# 跨裝置同步設定（GitHub Gist）

每台裝置貼一次 token，大約 2 分鐘。之後醫院電腦、家裡電腦、手機的紀錄就會自動互通。

紀錄會存成你 GitHub 帳號底下一個 **私密 Gist** 裡的 `cv_consult_records.json`，只有你自己看得到。

---

## 一、產生 token（只要做一次）

1. 登入 GitHub → 右上角頭像 → **Settings**
2. 左側選單拉到最下面 → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. 右上角 **Generate new token** → **Generate new token (classic)**
5. 填寫：
   - **Note**：隨便打，例如 `cv-consult`
   - **Expiration**：建議 `90 days` 或 `1 year`（到期後再產一組新的貼上就好）
   - **Select scopes**：**只勾 `gist` 這一項**，其他全部不要勾
6. 最下面 **Generate token**
7. 複製那串 `ghp_` 開頭的字 —— **離開這個頁面就再也看不到了**，先貼在記事本也行

> 只勾 `gist` 的 token 只能讀寫你的 Gist，碰不到你的任何程式碼儲存庫。

## 二、貼進工具

1. 打開 <https://sherry45682-ship-it.github.io/consultCV-/>
2. 點工具列上的雲端狀態鈕（寫著「● 同步未設定」），或「紀錄清單」→「跨裝置同步」
3. 把 token 貼進欄位 → 按 **連結並同步**
4. 顯示「已同步」就完成了

## 三、其他裝置

打開同一個網址，貼**同一組** token，按「連結並同步」即可 —— 它會自動找到同一個 Gist 並把紀錄拉下來。token 可以重複使用，不用每台裝置產一組。

手機也一樣（瀏覽器打開網址就能用，建議加到主畫面）。

---

## 同步規則

- **本機優先**：沒網路、沒貼 token 也能照常填寫，恢復連線後會自動補同步。
- **自動同步時機**：存檔／更新／刪除後約 2.5 秒、頁面重新打開、網路恢復、切回這個分頁（距上次超過 1 分鐘）。也可以隨時按「立即同步」。
- **同一筆在兩台改**：以 `updatedAt` 比較，**後改的那一版勝出**（不會兩版並存）。所以同一筆盡量不要同時在兩台裝置編輯。
- **刪除**：會留一個「刪除記號」同步出去，所以刪掉的不會被其他裝置推回來。記號保留半年。
- **不會遺失**：同步失敗（沒網路、token 過期、API 出錯）時本機資料完全不動，狀態列會顯示原因。

## 兩個清除按鈕的差別

| 按鈕 | 做什麼 |
|---|---|
| **刪除全部紀錄** | 真的刪除，Gist 和其他裝置下次同步後也會一起消失 |
| **離開這台電腦** | 忘掉 token + 清掉這台機器的本機資料，**Gist 上的紀錄保留**。公用電腦用完請按這個 |

## 常見問題

**狀態顯示「token 無效或已被撤銷」**
token 過期或被刪了。回第一步產一組新的，重新貼上即可，Gist 裡的資料不受影響。

**狀態顯示「token 沒有 gist 權限」**
產生 token 時忘了勾 `gist`。重新產一組，記得勾。

**新裝置貼了 token 卻看不到舊紀錄**
確認貼的是同一個 GitHub 帳號的 token（狀態列會顯示帳號名稱）。真的接不起來時，可以用舊裝置「匯出 JSON」、新裝置「匯入 JSON」搬過去。

**「GitHub API 次數上限」**
短時間打太多次 API，等幾分鐘就會恢復。

**想看雲端到底存了什麼**
同步面板裡有「在 GitHub 上看這個 Gist」的連結，點進去就能看到那個 JSON 檔（也可以在那裡下載備份）。

## 隱私提醒

- Gist 設為 **private（私密）**，只有你登入自己的帳號看得到；但私密 Gist 不是加密的，知道網址的人仍看得到內容，所以**不要在紀錄裡填姓名和病歷號**，只寫床號或代號。
- token 只存在該台裝置的瀏覽器裡（localStorage），不會上傳到任何地方；公用電腦用完請按「離開這台電腦」。
- 不放心的話，可以隨時到 GitHub 的 Tokens 頁面把那組 token 撤銷，所有裝置就立刻失效。
