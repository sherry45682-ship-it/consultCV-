# 跨裝置同步設定（Google 雲端硬碟）

只要做一次。設定完成後，每台裝置（醫院電腦、家裡電腦、手機）只要登入**同一個 Google 帳號**，紀錄就會自動互相同步。

資料會存成你自己雲端硬碟裡的一個檔案 `cv_consult_records.json`，不經過任何第三方伺服器。

---

## 一、建立 Google Cloud 專案

1. 打開 <https://console.cloud.google.com/>，用你要拿來同步的 Google 帳號登入。
2. 左上角專案選單 →「新增專案」，名稱隨意（例如 `cv-consult`）→ 建立。

## 二、啟用 Google Drive API

1. 左側選單「API 和服務」→「程式庫」。
2. 搜尋 **Google Drive API** → 點進去 → 「啟用」。

## 三、設定 OAuth 同意畫面

1. 「API 和服務」→「OAuth 同意畫面」。
2. User Type 選 **External（外部）** → 建立。
3. 應用程式名稱隨意（例如 `心肺照會病歷產生器`），使用者支援電子郵件和開發人員聯絡資訊填自己的 Gmail → 儲存並繼續。
4. 範圍（Scopes）那一頁不用加東西，直接「儲存並繼續」。
5. **測試使用者（Test users）**：按「+ ADD USERS」，把你自己的 Gmail 加進去（每台裝置要登入的那個帳號）→ 儲存。

> 維持在「測試」狀態就可以了，不需要送審。

## 四、建立 OAuth 用戶端 ID

1. 「API 和服務」→「憑證」→「+ 建立憑證」→「OAuth 用戶端 ID」。
2. 應用程式類型選 **網頁應用程式**。
3. 「已授權的 JavaScript 來源」按「+ 新增 URI」，填入網站來源（**只有網域，不能帶路徑**）：

   ```
   https://sherry45682-ship-it.github.io
   ```

   （打開網站後點「雲端同步」，說明裡會直接列出可以照抄的那一行，不用自己拼。）

4. 「已授權的重新導向 URI」**不用填**。
5. 建立 → 複製那串 `xxxxxxxx-xxxx.apps.googleusercontent.com`。

## 五、把用戶端 ID 放進工具

兩種方式，擇一：

**方式 A（推薦，每台裝置都不用再設定）**
打開 `index.html`，找到這一行（在後半段 `SYNC` 區塊的開頭）：

```js
const GOOGLE_CLIENT_ID = '';   /* ← 想寫死的話把用戶端 ID 貼在這裡（留空則用同步面板裡的設定） */
```

把 ID 貼進引號中間，存檔並 push 到 GitHub：

```js
const GOOGLE_CLIENT_ID = '123456789-abcdefg.apps.googleusercontent.com';
```

> 用戶端 ID 不是密碼，本來就會出現在網頁原始碼裡，公開沒有安全問題；真正的防線是第四步的「已授權的 JavaScript 來源」——別的網站就算拿到這串 ID 也用不了。

**方式 B（不想改檔案）**
開網站 →「紀錄清單」→「雲端同步」→ 把 ID 貼在欄位裡按「儲存」。缺點是**每台裝置都要各貼一次**。

## 六、開始用

1. 打開網站 → 點右上的雲端狀態鈕（或「紀錄清單」→「雲端同步」）。
2. 按 **「連結雲端硬碟」** → 選擇 Google 帳號 → 允許。
   - 如果出現「Google 尚未驗證這個應用程式」，點「進階」→「前往（不安全）」。這是你自己建立、只給自己用的應用程式，不會送審所以一定會有這個畫面。
3. 出現「已同步」就完成了。
4. 在第二台裝置打開同一個網址，一樣按「連結雲端硬碟」、登入**同一個 Google 帳號**，紀錄就會拉下來。

---

## 同步規則

- **本機優先**：沒網路、沒登入也能照常填寫，恢復連線後會自動補同步。
- **自動同步時機**：存檔／更新／刪除後約 2.5 秒、頁面重新打開、網路恢復、切回這個分頁（距上次超過 1 分鐘）。也可以隨時按「立即同步」。
- **同一筆在兩台改**：以 `updatedAt` 比較，**後改的那一版勝出**（不會兩版並存）。所以同一筆盡量不要同時在兩台裝置編輯。
- **刪除**：會留一個「刪除記號」同步出去，所以刪掉的不會被其他裝置推回來。記號保留半年。
- **不會遺失**：同步失敗（沒網路、權杖過期、API 出錯）時本機資料完全不動，狀態列會顯示原因。

## 兩個清除按鈕的差別

| 按鈕 | 做什麼 |
|---|---|
| **刪除全部紀錄** | 真的刪除，雲端和其他裝置下次同步後也會一起消失 |
| **離開這台電腦** | 登出雲端 + 清掉這台機器的本機資料，**雲端的紀錄保留**。公用電腦用完請按這個 |

## 常見問題

**按了沒反應／跳出 popup 被擋**
瀏覽器擋住彈出視窗。允許這個網站的彈出視窗後再按一次「立即同步」。

**狀態顯示「需重新登入」**
存取權杖一小時就到期，而瀏覽器不允許沒有經過點擊的自動登入。點一下「立即同步」即可，資料不會有事。

**新裝置同步後看不到舊紀錄**
先確認登入的是同一個 Google 帳號（狀態列會顯示 email）。真的接不起來時，可以用舊裝置「匯出 JSON」、新裝置「匯入 JSON」搬過去。

**錯誤訊息出現 `origin` 或 `idpiframe_initialization_failed`**
第四步的「已授權的 JavaScript 來源」沒填對。必須和網址列的來源完全一致、不能有結尾斜線或路徑。

**想換帳號**
「中斷連結」→ 再按「連結雲端硬碟」選另一個帳號。（本機紀錄會留著，下次同步會併到新帳號的檔案裡。）

## 隱私提醒

- 資料存在你自己的 Google 雲端硬碟，權限只要求 `drive.file`——這個程式只碰得到它自己建立的那一個檔案，看不到你雲端硬碟裡的其他東西。
- 存取權杖存在瀏覽器裡、一小時到期；公用電腦請按「離開這台電腦」。
- 還是建議紀錄標題只寫床號或代號，不要填姓名和病歷號。
